import java.util.*;
import java.lang.*;
import java.io.*;

class Codechef
{
	public static void main (String[] args) throws java.lang.Exception
	{
		Scanner sc = new Scanner(System.in);
		
		// Read the three integers: wins (A), draws (B), and losses (C)
		int A = sc.nextInt();
		int B = sc.nextInt();
		int C = sc.nextInt();
		
		// Calculate total points
		int totalPoints = (A * 3) + (B * 1);
		
		// Output the result
		System.out.println(totalPoints);
	}
}
